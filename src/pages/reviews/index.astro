---
import MainLayout from "@/layouts/MainLayout.astro";
import PlaceCard from "@/components/reviews/PlaceCard.astro";
import { actions } from "astro:actions";

const resp = await Astro.callAction(actions.getAllTest.orThrow, {})
console.log("resp:", resp)
//console.log("resp.data:", resp.data.dataRole)

---

<MainLayout title="Vengardus - Reviews">
  <h1 class="text-3xl text-blue-500">Reviews</h1>
  <div class="grid grid-cols-1 gap-x-4 gap-y-7 sm:grid-cols-3">
    <PlaceCard />
  </div>

</MainLayout>
